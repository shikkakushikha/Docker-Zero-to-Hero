apiVersion: v1
kind: Service
metadata:
  name: python-service
spec:
  type: NodePort
  # LoadBalancer when exposing to outside
  selector:
    app.kubernetes.io/name: python-sample-app-demo
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30007
